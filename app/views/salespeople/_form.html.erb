<%= form_with(model: salesperson) do |form| %>
  <% if salesperson.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(salesperson.errors.count, "error") %> prohibited this salesperson from being saved:</h2>

      <ul>
        <% salesperson.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :person_id, style: "display: block" %>
    <%= form.text_field :person_id %>
  </div>

  <div>
    <%= form.label :dealership_id, style: "display: block" %>
    <%= form.text_field :dealership_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
